# Tasks 002: Utopia DB Integration

- [x] Implement `UtopiaDBAdapter` in `src/adapters/utopia_db.py`.
- [x] Create dedicated Knowledge Base in Utopia DB (`01a08474-0000-7000-8000-000000000001`).
- [x] Support schema initialization (`init_schema()`) and intent registration (`register_intent()`).
- [x] Implement Knowledge Graph synchronization with `entities` and `facts`.
